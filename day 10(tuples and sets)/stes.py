# sets are unordered collections of unique elements. They are mutable, meaning you can add or remove elements after the set has been created. Sets are useful for operations like union, intersection, and difference.
# these are denoted by curly braces {} or the set() function.
# fuctions that can be used with sets include add(), remove(), discard(), pop(), clear(), union(), intersection(), difference(), symmetric_difference(), issubset(), issuperset(), and isdisjoint().

info = {"pavan",19,False,5.3,19}
print(info)

info = {"pavan",19,False,5.3,19}
for item in info:
    print(item)
    